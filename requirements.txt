# Flask web framework
Flask==3.0.0

# OpenAI Python SDK for API access
# Using version 1.12.0 for better stability and compatibility
openai==1.12.0

# Audio processing library for handling large files
# Note: Requires ffmpeg to be installed on the system
pydub==0.25.1

# WSGI server for production deployment
gunicorn==21.2.0

# Additional Flask dependencies (automatically installed with Flask)
Werkzeug==3.0.1
